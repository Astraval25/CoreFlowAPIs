meta {
  name: Delete Order by Order id
  type: http
  seq: 5
}

delete {
  url: {{astraval.com}}/api/companies/{{companyId}}/orders/0
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
