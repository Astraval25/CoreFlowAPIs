meta {
  name: Get sales order items
  type: http
  seq: 8
}

get {
  url: {{astraval.com}}/api/companies/{{companyId}}/customers/1/items
  body: text
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
