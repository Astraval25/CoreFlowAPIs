meta {
  name: Update Sales Order by Order id
  type: http
  seq: 6
}

put {
  url: {{astraval.com}}/api/companies/{{companyId}}/sales/orders/{{orderId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
