meta {
  name: Deactivate Order by Order id
  type: http
  seq: 5
}

put {
  url: {{astraval.com}}/api/companies/{{companyId}}/orders/{{orderId}}/deactivate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
