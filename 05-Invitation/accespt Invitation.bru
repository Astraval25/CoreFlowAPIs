meta {
  name: accespt Invitation
  type: http
  seq: 4
}

post {
  url: {{astraval.com}}/api/companies/{{companyId}}/invitations/:invitationCode/accept
  body: json
  auth: bearer
}

params:path {
  invitationCode: P89RUC
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "selectedCustomerId": 3
  }
}

vars:pre-request {
  invitationCode: 6MM3R3
}

settings {
  encodeUrl: true
  timeout: 0
}

example {
  name: accept by Vendor
  
  request: {
    url: {{astraval.com}}/api/companies/{{companyId}}/invitations/{{invitationCode}}/accept
    method: POST
    mode: json
    body:json: {
      {
        "selectedVendorId":2
      }
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}

example {
  name: accept by Customer
  
  request: {
    url: {{astraval.com}}/api/companies/{{companyId}}/invitations/{{invitationCode}}/accept
    method: POST
    mode: json
    body:json: {
      {
        "selectedCustomerId":2
      }
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
