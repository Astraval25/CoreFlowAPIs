meta {
  name: Send Invitation from Customer
  type: http
  seq: 1
}

post {
  url: {{astraval.com}}/api/companies/{{companyId}}/invitations/customers/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
