meta {
  name: Create Vendor Item
  type: http
  seq: 1
}

post {
  url: {{astraval.com}}/api/companies/{{companyId}}/vendors/1/items
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "itemId": 4,
    "purchasePrice":45.00,
    "purchaseDescription": "Price for vendor"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
