meta {
  name: Create Vendor Item
  type: http
  seq: 1
}

post {
  url: {{astraval.com}}/api/companies/{{companyId}}/vendors/1/items
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "itemId": 1,
    "purchasePrice":104.00,
    "purchaseDescription": "Special price for customer 3"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
