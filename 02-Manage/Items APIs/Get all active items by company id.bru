meta {
  name: Get all active items by company id
  type: http
  seq: 1
}

get {
  url: {{astraval.com}}/api/companies/{{companyId}}/items/active
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
