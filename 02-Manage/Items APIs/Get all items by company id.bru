meta {
  name: Get all items by company id
  type: http
  seq: 5
}

get {
  url: {{astraval.com}}/api/companies/{{companyId}}/items
  body: text
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
