meta {
  name: Activate Customer Item
  type: http
  seq: 4
}

patch {
  url: {{astraval.com}}/api/companies/{{companyId}}/customers/4/items/1/activate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
