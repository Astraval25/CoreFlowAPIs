meta {
  name: Get Item  by customer
  type: http
  seq: 6
}

get {
  url: {{astraval.com}}/api/companies/{{companyId}}/customers/1/items
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
