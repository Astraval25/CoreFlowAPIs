meta {
  name: Send Invitation from Vendor
  type: http
  seq: 2
}

post {
  url: {{astraval.com}}/api/companies/{{companyId}}/invitations/vendors/2
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
