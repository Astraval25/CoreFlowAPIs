meta {
  name: Get all active customer by company Id
  type: http
  seq: 1
}

get {
  url: {{astraval.com}}/api/companies/{{companyId}}/customers/active
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
