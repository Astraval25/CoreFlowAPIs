meta {
  name: Get customer detail by customer Id and company id
  type: http
  seq: 2
}

get {
  url: {{astraval.com}}/api/companies/{{companyId}}/customers/15
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  This API check the both company Id and Customer Id both want to match
}
